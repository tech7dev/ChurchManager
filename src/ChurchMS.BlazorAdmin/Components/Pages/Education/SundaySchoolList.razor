@page "/sunday-school"
@attribute [Authorize(Roles = "SuperAdmin,CentralAdmin,ChurchAdmin,Teacher")]
@inject EducationService EducationService

<PageTitle>Sunday School — ChurchMS</PageTitle>

<div class="cms-page-header">
    <div class="cms-page-header-left">
        <div class="cms-page-icon" style="background:rgba(255,152,0,0.1);">
            <MudIcon Icon="@Icons.Material.Outlined.MenuBook" Color="Color.Warning" />
        </div>
        <div class="cms-page-title">
            <MudText Typo="Typo.h5" Style="font-weight:700;line-height:1.2;">Sunday School</MudText>
            <MudText Typo="Typo.caption" Color="Color.Secondary">
                Manage classes, lessons, and student enrollment
            </MudText>
        </div>
    </div>
</div>

<MudPaper Elevation="1" Style="border-radius:12px;overflow:hidden;">
    <MudTable ServerData="@ServerLoad" Dense="false" Hover="true" @ref="_table"
              RowsPerPage="20" Loading="@_loading" LoadingProgressColor="Color.Warning"
              Elevation="0">
        <HeaderContent>
            <MudTh Style="font-weight:600;">Name</MudTh>
            <MudTh Style="font-weight:600;">Level</MudTh>
            <MudTh Style="font-weight:600;">Teacher</MudTh>
            <MudTh Style="font-weight:600;">Enrolled</MudTh>
            <MudTh Style="font-weight:600;">Active</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Name">
                <MudText Typo="Typo.body2" Style="font-weight:600;">@context.Name</MudText>
            </MudTd>
            <MudTd DataLabel="Level">
                <MudChip T="string" Size="Size.Small" Color="Color.Default"
                         Variant="Variant.Filled" Label="true"
                         Style="border-radius:6px;">
                    @context.Level
                </MudChip>
            </MudTd>
            <MudTd DataLabel="Teacher">
                <MudText Typo="Typo.body2">@(context.TeacherName ?? "—")</MudText>
            </MudTd>
            <MudTd DataLabel="Enrolled">
                <MudText Typo="Typo.body2">@context.EnrollmentCount</MudText>
            </MudTd>
            <MudTd DataLabel="Active">
                <MudChip T="string" Size="Size.Small"
                         Color="@(context.IsActive ? Color.Success : Color.Default)"
                         Variant="Variant.Filled" Label="true"
                         Style="border-radius:6px;">
                    @(context.IsActive ? "Active" : "Inactive")
                </MudChip>
            </MudTd>
        </RowTemplate>
        <NoRecordsContent>
            <div class="d-flex flex-column align-center pa-8 gap-2">
                <MudIcon Icon="@Icons.Material.Outlined.MenuBook"
                         Style="font-size:3rem;color:rgba(0,0,0,0.2);" />
                <MudText Color="Color.Secondary">No classes found.</MudText>
            </div>
        </NoRecordsContent>
        <LoadingContent>
            <MudText Align="Align.Center" Class="pa-4" Color="Color.Secondary">Loading…</MudText>
        </LoadingContent>
        <PagerContent>
            <MudTablePager />
        </PagerContent>
    </MudTable>
</MudPaper>

@code {
    private MudTable<SundaySchoolClassDto> _table = null!;
    private bool _loading;

    private async Task<TableData<SundaySchoolClassDto>> ServerLoad(TableState state, CancellationToken ct)
    {
        _loading = true;
        try
        {
            var result = await EducationService.GetSundaySchoolClassesAsync(
                state.Page + 1, state.PageSize);
            return new TableData<SundaySchoolClassDto>
            {
                Items = result?.Items ?? [],
                TotalItems = result?.TotalCount ?? 0
            };
        }
        finally { _loading = false; }
    }
}
